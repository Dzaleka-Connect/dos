---
interface Props {
  variant?: 'default' | 'elevated' | 'outlined' | 'ghost';
  padding?: 'none' | 'sm' | 'md' | 'lg';
  hover?: boolean;
  rounded?: 'sm' | 'md' | 'lg' | 'xl';
  class?: string;
}

const {
  variant = 'default',
  padding = 'md',
  hover = false,
  rounded = 'xl',
  class: className = ''
} = Astro.props;

const variantClasses = {
  default: 'border border-gray-200 bg-white',
  elevated: 'border border-gray-200 bg-white shadow-sm',
  outlined: 'bg-white border border-gray-200',
  ghost: 'bg-transparent'
};

const paddingClasses = {
  none: '',
  sm: 'p-4',
  md: 'p-6',
  lg: 'p-8'
};

const roundedClasses = {
  sm: 'rounded-md',
  md: 'rounded-lg',
  lg: 'rounded-xl',
  xl: 'rounded-xl'
};

const hoverClass = hover ? 'transition hover:border-gray-300 hover:shadow-sm' : '';

const cardClasses = `${variantClasses[variant]} ${paddingClasses[padding]} ${roundedClasses[rounded]} ${hoverClass} ${className}`;
---

<div class={cardClasses}>
  <slot />
</div>
